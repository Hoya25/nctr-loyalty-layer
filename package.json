{
  "name": "nctr-loyalty-layer",
  "version": "2.2.0",
  "private": true,
  "description": "NCTR Loyalty Layer \u2014 Cloudflare Worker behind api.nctr.live",
  "type": "module",
  "scripts": {
    "check": "node --check index.js",
    "dry-run": "wrangler deploy --dry-run --outdir=.wrangler-build",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail nctr-loyalty-layer --format pretty",
    "test": "node --test test/disclosure.test.mjs",
    "test:live": "node --test test/"
  },
  "devDependencies": {
    "wrangler": "^4.106.0"
  },
  "dependencies": {
    "viem": "2.47.12"
  }
}
